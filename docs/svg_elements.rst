SVG Elements
=================================

Classes that represent svg elements. These classes can be added to an ``SVG.SVG`` or ``SVG.Section`` to render those objects on that SVG.

SVG Elements are currently accessible through the Draw Module

Elements Include:

.. toctree::
    elements_circle
    elements_rectangle
    elements_path
