SVG Elements
=================================

Classes that represent svg elements. These classes can be added to an ``SVG.SVG`` or ``SVG.Section`` to render those objects on that SVG.

Elements Include:

.. toctree::
    elements_circle
    elements_rectangle
